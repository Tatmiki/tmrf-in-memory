#### Comando para baixar as dependências do projeto assumindo que você está no root do projeto:
- `pip install -e .` 

#### Script para baixar os datasets necessários:
- `python ./scripts/download_data.py`